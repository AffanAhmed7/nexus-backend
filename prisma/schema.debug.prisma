generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:manchester@localhost:5433/Portfolio?schema=public"
}

model User {
  id    String @id @default(uuid())
  email String @unique
}
